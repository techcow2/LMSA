document.addEventListener('DOMContentLoaded', () => {
    // This file is kept for backward compatibility but the functionality has been moved to event-handlers.js
    // The delete-all-confirmation-modal is no longer used to prevent duplicate confirmation dialogs
});
